import { ErrorZ } from "./error.zod.js";

/**
 * Validator objects follow the name of ApiNameZ. We use Z as we are using Zod
 * for the validator and Z makes it easy to identify and type.
 */
export const Validation = {
  ErrorZ,
};
