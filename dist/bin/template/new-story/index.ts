import { TemplateSync } from "../sync-template-to-target";

/**
 * This template doesn't require any file copies. These templates are specific
 * to being injected into an existing project file.
 */
export default async () => ({}) as TemplateSync;
